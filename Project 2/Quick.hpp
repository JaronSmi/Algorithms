static bool mycmp(coordinate_pair&, coordinate_pair&);
int testSide(coordinate_pair&, coordinate_pair&, coordinate_pair&);
void FindHull(std::vector<coordinate_pair>&, coordinate_pair&, coordinate_pair&);
std::vector<coordinate_pair> outerTrees(std::vector<coordinate_pair>&);