// Prototypes for Graham Scan
int compare(const void*, const void*);
int direction(coordinate_pair, coordinate_pair, coordinate_pair);
int squaredDist(coordinate_pair, coordinate_pair);
coordinate_pair second_top(std::stack<coordinate_pair> &);