// Prototypes for Jarvis March
std::set<coordinate_pair> jarvis_hull(std::vector<coordinate_pair> &, int);
int cross_product(coordinate_pair, coordinate_pair, coordinate_pair);
int distance(coordinate_pair, coordinate_pair, coordinate_pair);